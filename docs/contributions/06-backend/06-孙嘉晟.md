### 后端开发贡献说明

**姓名**：孙嘉晟

 **学号**：2312190130

**技术栈**：Node.js + Express + MySQL + Docker + JWT 

**日期**：2026-04-13

### 我完成的工作

#### 核心业务模块开发

[✓] **用户认证与鉴权模块**（基于 `jsonwebtoken` 实现登录态签发，完成注册查重、默认角色分配与明文/密文校验逻辑） 

[✓] **课程与进度管理模块**（实现课程的发布、下架、章节管理，以及关联学生表实现选课防重、视频播放进度记录） 

[✓] **社区互动模块**（设计热度算法处理帖子排序，实现多表联查加载评论，以及帖子点赞状态的动态切换

） [✓] **AI 助教模块**（系统级集成外部大模型能力，定制“栖学课堂”专属人设，实现流式/非流式的学习问答机制）

 [✓] **其他**：基于系统运维需求，实现标准化的 `/health` 健康检查端点及全局系统指标统计。

#### 中间件与架构封装

- **模块 1：全局数据库连接池**（`db.js`，基于 `mysql2/promise` 封装高并发连接池，支持 Docker 内部网络通信及异步 SQL 执行）
- **模块 2：文件上传中间件**（基于 `multer` 配置磁盘存储策略，封装时间戳重命名防重机制，对外暴露静态资源服务 `/uploads`）
- **模块 3：日志与指标收集中间件**（`utils/logger`，全局拦截并计算 API 请求耗时，依据 HTTP 状态码自动分流记录普通请求与异常错误）
- **模块 4：跨域与解析中间件**（配置 `cors` 解决前后端分离同源策略限制，集成 `express.json` 实现 RESTful 请求体自动解析）

#### API 接口开发

[✓] **课程体系接口**（提供 `GET /api/courses`、`GET /api/courses/:id/chapters` 等列表与详情接口，供前端渲染课程库）

 [✓] **用户与交互接口**（编写 `GET /api/user/profile/:id` 个人书斋接口，联表查询用户的随堂时光笔记及课程评价） 

[✓] **发帖与评论接口**（提供 `POST /api/posts` 及相关评论、点赞接口，支持管理员级联删除操作） [✓] **第三方能力接口**（`POST /api/chat`，封装 HTTP 请求调用 DeepSeek API，拦截并处理外部请求异常状态）

#### 其他核心工作

- **数据库设计与初始化**（设计并创建 `users`, `courses`, `posts`, `enrollments` 等 12 张核心数据表，确立主外键级联删除关系与字符集规范）
- **容器化部署方案**（编写 `Dockerfile` 与 `docker-compose.yml`，编排 Node.js 后端与 MySQL 8.0 容器，实现数据卷持久化挂载）
- **环境与网关配置**（配置云服务器防火墙/安全组，打通内网与外网 3000 端口通信，处理宿主机与容器间的网络隔离问题）
- **代码规范与维护**（统一前后端 JSON 返回体格式 `{ success: boolean, message: string, data: any }`，提升前后端联调效率）

**代码仓库链接**： [https://github.com/sssttt11/online-learning-platform/tree/main/backend](https://www.google.com/search?q=https://github.com/sssttt11/online-learning-platform/tree/main/backend)