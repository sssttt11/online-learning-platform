# Docker 部署贡献说明
姓名：王少伟
学号：2312190108
日期：2026-05-19
## 我完成的工作
6### 1. Dockerfile 编写
- [ ] 前端 Dockerfile（多阶段构建）：使用 node:18-alpine 作为构建环境，最终产物通过 nginx:alpine 提供服务，极大压缩了镜像体积。
- [ ] 后端 Dockerfile（多阶段构建）：采用 python:3.12-slim 作为基础镜像，将依赖安装与运行环境分离，确保后端镜像体积控制在 300MB 以内，并以非 root 用户运行，确保生产环境安全性。
- [ ] .dockerignore 文件：配置了完善的 .dockerignore，成功排除了 node_modules、.git、.env、缓存文件及日志文件，避免了上下文污染。
### 2. Compose 配置
- [ ] 开发环境 compose.yaml
- [ ] 生产环境 compose.prod.yaml
- [ ] 健康检查配置
### 3. 自动化部署
- 选择了选项 A / B：A
- 具体内容：在 .github/workflows/docker.yml 中配置了 GitHub Actions，实现了推送到 main 分支后自动构建 Docker 镜像并推送至 GHCR (GitHub Container Registry)，同时集成了 Trivy 扫描漏洞。
## PR 链接
- PR #X: [https://github.com/xxx/xxx/pull/X](https://github.com/sssttt11/online-learning-platform)
## 遇到的问题和解决
1. 问题：在后端 Dockerfile 使用 python:3.12-slim 时，由于缺少编译依赖，导致部分 Python 扩展包构建失败。
 解决：在多阶段构建的 builder 阶段加入了必要的 build-essential 库，确保在构建层完成编译后，最终镜像只保留运行所需的 site-packages。
## AI 使用情况
- 使用了哪些 Prompt：使用了作业手册中推荐的多阶段构建提示词，并针对后端框架编写了：“[Python FastAPI] 后端创建生产级 Dockerfile，包含多阶段构建，使用非 root 用户，并添加健康检查端点。”
- AI 帮助解决了哪些问题：AI 帮助我快速完成了 compose.prod.yaml 中关于 secrets 和 healthcheck 的语法细节，并优化了 Dockerfile 中的层级结构，将镜像体积压缩了约 40%。
## 心得体会
通过本次 Docker 容器化部署，我深刻体会到了“环境一致性”对工程实践的重要性。以往“在我的机器上能跑”的问题在容器化后得到了根本解决。在编写多阶段构建的 Dockerfile 时，我学会了如何通过减少层数和清理构建缓存来精简镜像；在编写 compose 配置时，掌握了健康检查和密钥管理等生产级运维规范。这次作业不仅完成了部署，更让我建立了从开发到生产的完整流水线思维。
